import logo from '../../images/qr-code.png';

const Header = () => {
  return (
    <header>
      QRCode Generator
      <img src={logo} alt="app logo" />
    </header>
  )
}

export default Header;
